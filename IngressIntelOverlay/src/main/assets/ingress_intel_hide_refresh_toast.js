javascript:

(function(global) {

    var refreshToast = document.getElementById("butterbar");

    if (refreshToast != null) {
        refreshToast.style.top = "-10%";
        refreshToast.style.bottom = "110%";
    }

})((this || 0).self || global);
