javascript:

(function(global) {
    var target;
    target = document.getElementById('loading_msg');
    target.style.top = "0px";
    target = document.getElementById('loading_msg_text');
    target.style.top = "0px";
})((this || 0).self || global);

